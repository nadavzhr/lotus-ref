/**
 * Sidebar — initially collapsed / hidden.
 * Architecture-ready placeholder for Phase 6 (file tree, outline, search).
 */
export function Sidebar() {
  // Phase 6: render sidebar content here.
  // Currently returns null (no sidebar rendered).
  return null;
}
